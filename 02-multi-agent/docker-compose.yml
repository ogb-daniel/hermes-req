services:
  multi-agent:
    build: .
    environment:
      - OPENROUTER_API_KEY=${OPENROUTER_API_KEY}
    volumes:
      - ./mem:/app/mem
      - ./output:/app/output
    stdin_open: true
    tty: true
