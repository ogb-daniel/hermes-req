services:
  multi-agent:
    build: .
    environment:
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
    volumes:
      - ./mem:/app/mem
      - ./output:/app/output
    stdin_open: true
    tty: true
